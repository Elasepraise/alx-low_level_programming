  
#include "holberton.h"

/**
 * _isdigit - checks for lowercase character.
 * @m: character recibol for main.
 * Return: 1 if c is uppercase
 */
int _isdigit(int c)
{
	if (m >= '0' && m <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
